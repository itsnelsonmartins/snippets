import React from 'react';

const Modal = ({ placeholder, modalStatus }) => {
    return (
        <div className='modal'>
            <p>{placeholder}</p>
        </div>
    );
};

export default Modal;
